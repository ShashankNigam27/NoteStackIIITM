document.addEventListener("DOMContentLoaded", () => {
    // Hero Section Paper Plane Animations Trigger
    // Hover effect removed as per user request. 
    // Planes now only perform the initial fly-in animation.
});